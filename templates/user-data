#cloud-config
hostname: %%HOSTNAME%%
users:
  - name: %%USERNAME%%
    groups: wheel%%SUDOAS%%
    ssh_authorized_keys:
      - %%SSH-KEY%%
%%PACKAGES%%
%%SUDOASPACKAGE%%
growpart:
  mode: auto
  devices: ['/']

resizefs: true

%%RUNCMD%%
